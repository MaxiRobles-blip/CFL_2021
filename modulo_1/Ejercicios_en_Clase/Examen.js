let readlineSync = require('readline-sync');

let textoIngresado = readlineSync.question("Ingrese texto:");
textoIngresado = textoIngresado.replace(/\s+/g, '');
let tamañoArreglo = textoIngresado.length;

function contarCadaLetra (textoIngresado){
    
    let indice = 0;
    
    for (indice = 0; indice < tamañoArreglo; indice ++){
        if (textoIngresado[indice] == )

    }
}


console.log(textoIngresado);
